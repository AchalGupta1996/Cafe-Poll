<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add a New Cafe</title>

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">

    <style>
        body {
            background-color: #f8f9fa; /* Set a consistent background color */
        }

        /* Center the content */
        .container {
            margin-top: 50px;
        }

        h1 {
            text-align: center;
        }

        form {
            max-width: 400px;
            margin: 0 auto;
            background-color: #ffffff; /* Set a consistent form background color */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        input {
            margin-bottom: 15px;
            width: 100%;
            padding: 10px;
            box-sizing: border-box;
            border: 1px solid #ced4da; /* Set a consistent border color */
            border-radius: 5px;
        }

        button {
            background-color: #007bff; /* Set a consistent button background color */
            color: #ffffff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            
        }

        button:hover {
            background-color: #0056b3; /* Change button color on hover */
        }

        /* Style the navigation bar */
        nav {
            background-color: #007bff;
            padding: 10px 0;
        }

        nav a {
            color: #ffffff;
            margin: 0 10px;
            text-decoration: none;
        }

        /* Style the footer */
        footer {
            background-color: #343a40;
            color: #ffffff;
            text-align: center;
            padding: 10px 0;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="#">Cafe Poll</a>
        <!-- Add more navigation links if needed -->
    </nav>

    <!-- Content -->
    <div class="container">
        <h1>Add a New Cafe</h1>

        <form action="/add" method="post">
            <input type="text" name="name" placeholder="Enter cafe name">
            <input type="phone" name="phone" placeholder="Enter cafe phone no.">
            <button type="submit">Add Cafe</button>
        </form>
    </div>

    <!-- Footer -->
    <footer>
        &copy; 2024 Cafe Poll. All Rights Reserved.
    </footer>

    <!-- Bootstrap JS (optional) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
